git config --global user.name <name> // Для всех проектов задать имя
git config --global user.email <mail> // Для всех проектов задать почту

git init // Создаем локальный GIT репозиторий
ls -a // выводит все файлы в директории проекта

Основные команды:
git status  // Состояние текущего репозитория. Красным отображаются файлы которые не отслеживаются гитом
git add . // Зафиксировать текущее состояние всех файлов в папке проекта и отправить в коммит

git commit -m "название коммита"    // передать коммит

Команды с гита:
…or create a new repository on the command line
echo "# first_repo" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/smskorobogatov/first_repo.git
git push -u origin main

git subtree push --prefix public origin gh-pages


Порядок команд при изменение в проекте
git add . 
git commit -m "название коммита" 
git push